name: Build DuyKhanhOS ISO

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-os:
    runs-on: ubuntu-latest

    steps:
    # 1. Kéo toàn bộ mã nguồn từ Repo GitHub của bạn vào máy ảo Build
    - name: Checkout repository code
      uses: actions/checkout@v4

    # 2. Cài đặt các công cụ Linux đóng gói và thư viện đồ họa X11 để biên dịch duykhanhwm.c
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          debootstrap \
          squashfs-tools \
          xorriso \
          grub-pc-bin \
          grub-efi-amd64-bin \
          mtools \
          libx11-dev \
          build-essential

    # 3. Cấp quyền thực thi cho các file script hệ thống
    - name: Make scripts executable
      run: |
        chmod +x iso_builder.sh
        if [ -f duykhanh-fetch.sh ]; then chmod +x duykhanh-fetch.sh; fi

    # 4. Kích hoạt file iso_builder.sh để bắt đầu quá trình dựng hệ điều hành
    - name: Run ISO Builder Script
      run: |
        sudo ./iso_builder.sh

    # 5. Đóng gói file ISO thành phẩm và đẩy lên GitHub để bạn tải xuống
    - name: Upload ISO Artifact
      uses: actions/upload-artifact@v4
      with:
        name: DuyKhanhOS-ISO
        path: |
          *.iso
          live_boot/*.iso
        if-no-files-found: error
        retention-days: 7
