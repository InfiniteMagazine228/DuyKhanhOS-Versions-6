set timeout=5
set default=0

# Thiết kế màu sắc giao diện menu GRUB (Chữ/Nền) - Giữ nguyên phong cách của bạn
set menu_color_normal=cyan/black
set menu_color_highlight=white/blue

# Chế độ chạy trực tiếp từ ISO (Live Mode) - Tự động nhận diện phân vùng đĩa ảo
menuentry "DuyKhanhOS v6 (Live Boot - Mint Core)" {
    search --set=root --file /live/filesystem.squashfs
    linux /live/vmlinuz boot=live quiet splash
    initrd /live/initrd.img
}

# Chế độ cứu hộ đồ họa cơ bản cho Live ISO
menuentry "DuyKhanhOS v6 (Safe Graphics Mode)" {
    search --set=root --file /live/filesystem.squashfs
    linux /live/vmlinuz boot=live xforcevesa nomodeset quiet splash
    initrd /live/initrd.img
}
